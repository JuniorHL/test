{% extends 'base.html.twig' %}

{% block title %}Imagenes!{% endblock %}

{% block body %}
    {{ form(form) }}
    {% for imagen in imagenes %}
        <li> <img src="{{ asset('img/' ~ imagen.archivo) }}" width="250" height="250"> </li>
    {% endfor %}
    <div>HOla</div>
{% endblock %}
